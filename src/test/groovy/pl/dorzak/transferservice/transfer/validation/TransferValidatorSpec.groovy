package pl.dorzak.transferservice.transfer.validation

import spock.lang.Specification

/**
 * Only a placeholder indicating that there should be a tests
 */
class TransferValidatorSpec extends Specification {
    def "validate insert should do something"() {
    }

    def "validate update should do something"() {
    }
}
