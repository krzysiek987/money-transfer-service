package pl.dorzak.transferservice.transfer

import spock.lang.Specification

/**
 * Only a placeholder indicating that there should be a tests
 */
class TransferAPIImplSepc extends Specification {
    def "getBySourceAccountId should do something"() {
    }

    def "getAllScheduledToInvoke should do something"() {
    }

    def "findById should do something"() {
    }

    def "insert should do something"() {
    }

    def "update should do something"() {
    }

    def "invoke should do something"() {
    }

    def "setStatus should do something"() {
    }

    def "deleteById should do something"() {
    }
}
